# Documentation Rules

Update documentation when:

- architecture changes;
- schema changes;
- label definition changes;
- feature definition changes;
- model promotion occurs;
- deployment process changes.

Measured results must identify:

- dataset version;
- model version;
- validation period.

Do not write unsupported resume claims.
